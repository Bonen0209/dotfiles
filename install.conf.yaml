- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.tmux.conf:
    ~/.vimrc:
    ~/.zimrc:
    ~/.zshrc:
    ~/.zlogin:
    ~/.zshenv:

- shell:
  - git submodule update --init --recursive
